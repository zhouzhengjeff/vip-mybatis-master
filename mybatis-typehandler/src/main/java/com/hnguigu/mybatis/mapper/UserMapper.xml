<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hnguigu.mybatis.mapper.UserMapper">

    <insert id="save">
        INSERT INTO MYBATIS_USER(USER_ID, USER_NAME, USER_GENDER)
        VALUES (NULL, #{name}, #{gender})
    </insert>

    <insert id="insert">
        INSERT INTO MYBATIS_USER_1(USER_ID, USER_NAME, USER_GENDER)
        VALUES (NULL, #{name}, #{gender})
    </insert>


</mapper>